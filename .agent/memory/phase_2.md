Shannon entropy detector implemented in `@devs/secret-masker`. Thresholds: 20 chars, >4.5 bits.

Regex pattern library implemented: SECRET_PATTERNS in `@devs/secret-masker`. ≥100 patterns. See `packages/secret-masker/docs/patterns.md`. 
