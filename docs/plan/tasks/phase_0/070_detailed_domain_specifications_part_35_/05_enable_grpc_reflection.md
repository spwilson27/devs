# Task: Enable gRPC Reflection (Sub-Epic: 070_Detailed Domain Specifications (Part 35))

## Covered Requirements
- [2_TAS-REQ-429]

## Dependencies
- depends_on: [none]
- shared_components: ["devs-proto", "devs-server"]

## 1. Initial Test Written
- [ ] Start the `devs-server` (or a mock gRPC server if needed).
- [ ] Run `grpcurl -plaintext <address> list` and verify that it returns an error or empty list before reflection is enabled.
- [ ] Implement a test (unit or integration) that starts the server and verifies that the `devs.v1.*` services are discoverable via `ReflectionService`.

## 2. Task Implementation
- [ ] Add the `tonic-reflection` crate to the `devs-server/Cargo.toml` dependencies.
- [ ] Create a `tonic_reflection::server::Builder` and register all six required services.
- [ ] Ensure that the `FILE_DESCRIPTOR_SET` generated by `tonic-build` includes all the necessary gRPC descriptors.
- [ ] Configure the `devs-server` to serve the `ReflectionService` alongside the other services.
- [ ] Verify that `grpcurl list <server>` returns the following service names:
    - `devs.v1.WorkflowDefinitionService`
    - `devs.v1.RunService`
    - `devs.v1.StageService`
    - `devs.v1.LogService`
    - `devs.v1.PoolService`
    - `devs.v1.ProjectService`

## 3. Code Review
- [ ] Confirm that all gRPC services are properly registered with the reflection server.
- [ ] Verify that the `tonic-reflection` integration is correctly implemented.

## 4. Run Automated Tests to Verify
- [ ] Run the reflection integration test and confirm that `grpcurl` can list all services.

## 5. Update Documentation
- [ ] Document the availability of gRPC reflection and how to use `grpcurl` with the `devs` server.

## 6. Automated Verification
- [ ] Run `grpcurl -plaintext localhost:<port> list` (after starting the server in a test context) and confirm all six service names are present.
