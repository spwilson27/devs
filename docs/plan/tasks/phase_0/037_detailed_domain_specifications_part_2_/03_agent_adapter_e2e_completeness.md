# Task: Agent Adapter E2E Completeness KPI (Sub-Epic: 037_Detailed Domain Specifications (Part 2))

## Covered Requirements
- [1_PRD-KPI-BR-010]

## Dependencies
- depends_on: [none]
- shared_components: [Traceability & Verification Infrastructure]

## 1. Initial Test Written
- [ ] Create a Python test suite `tests/test_adapter_completeness_kpi.py` that:
    - Verifies that each of the five MVP adapters (`claude`, `gemini`, `opencode`, `qwen`, `copilot`) is checked for E2E test coverage.
    - Verifies that if any adapter lacks a passing E2E test, the KPI is reported as failing.
    - Verifies that the KPI only passes when all five adapters are covered by E2E tests.

## 2. Task Implementation
- [ ] Update the traceability scanner or create a dedicated script (`.tools/verify_adapter_completeness.py`) to specifically monitor E2E test coverage for agent adapters.
- [ ] Define the criteria for "passing E2E integration test" for an adapter (e.g., a test file in `tests/e2e/adapters/` with specific requirement tags).
- [ ] Integrate this check into the final verification report generated by `./do presubmit`.
- [ ] Ensure that a failure in this KPI prevents the overall presubmit status from being marked as successful.

## 3. Code Review
- [ ] Verify that the adapter completeness check is robust and cannot be satisfied by simple unit tests or empty test shells.
- [ ] Confirm that the adapter list in the script is exhaustive for the MVP scope (Claude, Gemini, OpenCode, Qwen, Copilot).

## 4. Run Automated Tests to Verify
- [ ] Run `python3 -m pytest tests/test_adapter_completeness_kpi.py`.
- [ ] Verify that the script correctly flags a missing adapter test.

## 5. Update Documentation
- [ ] Update the `README.md` in `.tools/` or `docs/plan/specs/1_prd.md` to explain the adapter completeness KPI and how to satisfy it.

## 6. Automated Verification
- [ ] Run the completeness scanner against the current codebase and verify its output matches the actual state of adapter testing.
