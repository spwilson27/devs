# Task: Verify SSH E2E Localhost Integration (Sub-Epic: 27_Risk 008 Verification)

## Covered Requirements
- [AC-RISK-008-03]

## Dependencies
- depends_on: [none]
- shared_components: [./do Entrypoint Script, devs-executor]

## 1. Initial Test Written
- [ ] Create a new E2E test file `tests/e2e/ssh_localhost_test.rs` that:
    - Uses a `StageID` with `Executor::SSH` target.
    - Configures the SSH address to `localhost`.
    - Specifies a trivial agent command (`echo ok`).
    - Asserts that the stage results in `StageStatus::Completed`.
    - Verifies that the stdout contains "ok".
    - Adds `// Covers: AC-RISK-008-03` annotation to the test.

## 2. Task Implementation
- [ ] In `devs-executor/src/ssh.rs` (or where SSH executor is defined):
    - Ensure that `localhost` is handled correctly using the test keys generated by `./do setup`.
    - Ensure that any `ssh_config` points to the test keys at `~/.ssh/devs_test_key`.
- [ ] In the E2E test implementation:
    - Set up a mock agent for the `echo ok` command if the SSH executor requires an agent binary to exist on the remote host (in this case, `localhost`).
    - Or ensure that the environment where the E2E test runs has SSH access enabled for the current user to `localhost` (e.g., via the authorized keys setup by `./do setup`).
- [ ] Verify that the test correctly handles the SSH connection timeout and retry logic as specified in `AC-RISK-008-03`.

## 3. Code Review
- [ ] Confirm that the test uses the literal string `echo ok` as required by `AC-RISK-008-03`.
- [ ] Confirm that the status assertion is strictly on `Completed`.
- [ ] Ensure that the test can run successfully in the `presubmit-linux` CI environment.

## 4. Run Automated Tests to Verify
- [ ] Run `./do setup` to ensure SSH keys are provisioned.
- [ ] Run `cargo test --test ssh_localhost_test --features e2e_ssh`.

## 5. Update Documentation
- [ ] Update `GEMINI.md` to record the verification of `AC-RISK-008-03`.

## 6. Automated Verification
- [ ] Run `.tools/verify_requirements.py` and ensure `AC-RISK-008-03` is marked as verified.
