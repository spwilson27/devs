{
  "name": "devs",
  "version": "0.1.0",
  "private": true,
  "description": "Agentic AI system that generates complete, tested greenfield software projects.",
  "devs": {
    "version": "1.0.0",
    "status": "development",
    "architecture": "monorepo"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "lint": "pnpm -r lint",
    "fmt": "pnpm -r fmt"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild"
    ]
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
