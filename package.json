{
  "name": "devs",
  "version": "0.1.0",
  "private": true,
  "description": "Agentic AI system that generates complete, tested greenfield software projects.",
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "fmt": "pnpm -r fmt"
  },
  "devDependencies": {}
}
