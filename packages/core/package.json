{
  "name": "@devs/core",
  "version": "0.1.0",
  "description": "Core orchestration logic: LangGraph state machine, agent coordination, and headless-first API.",
  "type": "module",
  "main": "./src/index.ts",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "echo 'build: not yet configured'",
    "test": "vitest run",
    "lint": "echo 'lint: not yet configured'",
    "fmt": "echo 'fmt: not yet configured'"
  },
  "dependencies": {
    "better-sqlite3": "^12.6.2",
    "fs-extra": "^11.3.3",
    "simple-git": "^3.32.0",
    "upath": "^2.0.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/fs-extra": "^11.0.4",
    "vitest": "^4.0.18"
  }
}
