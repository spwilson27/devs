{
  "name": "@devs/mcp",
  "version": "0.1.0",
  "description": "MCP (Model Context Protocol) server exposing debugging, profiling, and project introspection tools to AI agents.",
  "type": "module",
  "main": "./src/index.ts",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "echo 'build: not yet configured'",
    "test": "echo 'test: not yet configured'",
    "lint": "echo 'lint: not yet configured'",
    "fmt": "echo 'fmt: not yet configured'"
  },
  "dependencies": {
    "@devs/core": "workspace:*"
  },
  "devDependencies": {}
}
