{
  "name": "@devs/memory",
  "version": "0.1.0",
  "description": "Agent memory layer: short-term task context, medium-term phase decisions, long-term project constraints. SQLite-backed persistence.",
  "type": "module",
  "main": "./src/index.ts",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "echo 'build: not yet configured'",
    "test": "echo 'test: not yet configured'",
    "lint": "echo 'lint: not yet configured'",
    "fmt": "echo 'fmt: not yet configured'"
  },
  "dependencies": {},
  "devDependencies": {}
}
